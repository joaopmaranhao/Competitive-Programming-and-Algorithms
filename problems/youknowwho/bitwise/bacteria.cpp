#include <bits/stdc++.h>

#define _ ios_base::sync_with_stdio(0);cin.tie(0);
#define endl '\n'
using namespace std;

using ll = long long;

int main() { _

    ll n;
    cin >> n;
    cout << __builtin_popcountll(n) << endl;

    return 0;
}