#include <bits/stdc++.h>
#define _ ios_base::sync_with_stdio(0);cin.tie(0);
#define endl '\n'
#define pb push_back
#define all(x) (x).begin(), (x).end()

using namespace std;

typedef long long ll;
typedef unsigned long long llu;

const int INF = 0x3f3f3f3f;
const ll LINF = 0x3f3f3f3f3f3f3f3fll;

pair<vector<ll>, vector<ll>> solution;

vector<ll> curra;
vector<ll> currb;

int diff(vector<ll> a, vector<ll> b){
        return accumulate(all(a), 0LL) - accumulate(all(b), 0LL);
}

void backtrack(pair<vector<ll>, vector<ll>> sol){
        if(){

        }
        for(int i = 0; i < )
        if(diff(sol.first, sol.second) > diff(curra, currb)) continue;
}

int main(){ _
    
        return 0;
}