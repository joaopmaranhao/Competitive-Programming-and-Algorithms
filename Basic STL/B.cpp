#include <bits/stdc++.h>
#define _ ios_base::sync_with_stdio(0);cin.tie(0);
#define endl '\n'
#define pb push_back
#define all(x) (x).begin(), (x).end()

using namespace std;

using ll = long long;
using llu = unsigned long long;

int main(){
        int n;
        ll temp;
        vector<ll> seq;
        vector<ll> freq;

        cin >> n;

        for(int i = 0; i < n; i++){
                cin >> temp;
                seq.pb(temp);
        }

        for(int i = 0; i < n; i++){
                freq[]
        }
}